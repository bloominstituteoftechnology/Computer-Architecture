# push
# pop
# peek (look at the item on the top of the stack)
# is_empty (is the stack empty)

# Lets 
# store the stack data in RAM
# use R7 to hold the address of the top of the stack
    # get initialized to 0xf4 when the machine boots up

# So we really need is just a space to hold data and a stack pointer to point at the data

  v 
0 1 2 3 4 5 6 7 8 Index 
5 7 2 0 0 0 0 0 0 Value 

push 5
push 7 
push 2 
pop 2